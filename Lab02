#include <iostream>
using namespace std;

int main()
{
	//ZAD 1
	/*int n, temp;
	float pkt, suma = 0;
	cout << "Podaj liczbe studnetow ", cin >> n;
	temp = n;
	while (n > 0) 
	{
		cout << "Podaj ilosc pkt: ", cin >> pkt;
		suma += pkt;
		n--;
	}
	cout << "Srednia ilosc pkt: " << suma << endl;
	cout << "srednia ilosc pkt: " << suma / temp;
	*/

	//ZAD 2

	/*int n, temp;
	float pkt, suma = 0;
	cout << "podaj liczbe studnetow ", cin >> n;
	temp = n;
	while (n > 0)
	{
		cout << "podaj ilosc pkt: ", cin >> pkt;
			if (pkt >= 0 && pkt <= 100)
		{
			suma += pkt;
			n--;
		}
	}
	cout << "srednia ilosc pkt: " << suma << endl;
	cout << "srednia ilosc pkt: " << suma / temp;
	*/

	//ZAD 3

	/*int n, temp;
	float pkt, suma = 0;
	cout << "Podaj liczbe studnetow ", cin >> n;
	temp = n;
	while (n > 0)
	{
		cout << "Podaj ilosc pkt: ", cin >> pkt;

		if (pkt >= 0 && pkt <= 100)
		{
			suma += pkt;
			n--;
		}
		else continue;
	}
	cout << "Suma pkt: " << suma << endl;
	cout << "srednia ilosc pkt: " << suma / temp;
	*/

	//ZAD 4
	int liczba, ile_d = 0, ile_u = 0, suma_d = 0, suma_u = 0;
	for (int i = 0; i < 10; i++)
	{
		cout << "podaj liczbe ", cin >> liczba;
	}
	if (liczba >= 0)
	{
		ile_d++; suma_d += liczba;
	}
	else {
		ile_u++; suma_u += liczba;


	}
	cout << "ilosc liczb dodatnich " << ile_d << ", suma l dodatnih: " << suma_d << endl;
	cout << "ilosc liczb ujemnych " << ile_u << ", suma l ujemnych: " << suma_u << endl;
}
